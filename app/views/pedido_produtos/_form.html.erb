<%= form_with(model: pedido_produto, local: true) do |form| %>
  <% if pedido_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pedido_produto.errors.count, "error") %> prohibited this pedido_produto from being saved:</h2>

      <ul>
      <% pedido_produto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pedido_id %>
    <%= form.text_field :pedido_id %>
  </div>

  <div class="field">
    <%= form.label :produto_id %>
    <%= form.select :produto_id, Produto.all.collect{|p| [p.nome, p.id]}, {}%>
  </div>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
